<div>
  <h2  style="color: red;">json of products</h2>
 {{ product | json}}
 hallo11
  <div>
    product.title  - string - {{product.title}}
  </div>
  <div>
    product.variants  - string - {{product.variants | json}}
  </div>
  <div style="border: 1px solid red;">
    product.variants1  - string - {{product.variants[0] | json}}
  </div>
  <div>
    product.variants1  - string - {{product.variants[1].title}}
  </div>
  <div style="border: 1px solid red;">
    {{product.variants.size}}
  </div>
  {% if product.variants.size > 0 %}
  <ul>
    {% for variant in product.variants %}
    <li>
      {{variant.title}}
    </li>
      
    {% endfor %}
  </ul>
    
  {% endif %}
</div>
<h2  style="color: red;">json of collections</h2>

{% if section.settings.collection != blank %}
 {% assign collection = section.settings.collection %}
 <p>title</p> 
    <h2>{{collection.name}}</h2>
    <h2>
      Collection  object ={{ collection.title }}
    </h2>
    <div style="border: 1px solid green; padding: 2px;">
      {% for product in collection.products %}
        <div style="border: 1px solid red; padding: 2px;">
          <h3> Product  object = {{ product.title }}</h3>
          {% if product.variants.size > 0 %}
            <div style="border: 1px solid orange; padding: 2px;">
              {% for variant in product.variants %}
                <div style="border: 1px solid rgb(212, 89, 186); padding: 2px;">
                  <h4> Variant object = {{ variant.title }}</h4>
                  <ul>
                  {% for option in variant.options %}
                      <li style="border: 1px solid blue; padding: 2px;">
                       Option object {{ option }}
                      </li>
                  {% endfor %}
                  </ul>
                </div>
              {% endfor %}
            </div>
          {% else %}
            <div style="border: 1px solid purple; padding: 2px;">
                NONE
            </div>
          {% endif %}
        </div>
      {% endfor %}    
    </div>
{% endif %}


<script>
  console.log(`{{section.settings.collection | json }}`)
</script>
{% schema %}
{
  "name": "Product object",
  "tag": "section",
  "class": "section page-width",
  "settings": [
   {
    "type": "collection",
    "id": "collection",
    "label": "Collection"
   }
  ],
  
  "presets": [
    {
      "name": "Product object"
    }
  ]
}
{% endschema %}
